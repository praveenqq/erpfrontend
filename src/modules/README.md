# Business modules

Tenant-scoped features gated by `@RequiresModule` on the backend.

| Folder | Module code |
|--------|-------------|
| `expenses/` | `EXPENSES` |

Add new modules here with the same layout as `platform/`.
